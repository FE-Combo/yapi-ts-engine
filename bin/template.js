const generate = require('yapi-ts-engine');

generate({
  serverUrl: 'https://yapi.pro',
  projectId: '99',
  servicePath: 'src/services',
  requestImportExpression: "import ajax, { BaseResult } from 'src/utils/ajax';",
  hiddenBodyInGET: true,
  apiRename: (name) => {
    return name.replace(/^(\/v1\.\w\/test)/, '').replace(/\.\w\/test/, '');
  },
  shouldGenerateStaticMockData: true,
  hiddenTypes: ['headers'],
  responseType: (type) => {
    return `BaseResult<${type}>`;
  },
  additionalPageHeader: `/*
  Attention: This file is generated by "yapi-ts-engine", do not modify
*/\n`
});